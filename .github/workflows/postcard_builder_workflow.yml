on:
  workflow_call:
  inputs:
      postcard_template:
        required: true
        type: string
      postcard_reciever_name:
        required: true
        type: string

jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
    - name: ⚙️ Setup Imagemagick
        uses: mfinelli/setup-imagemagick@67fecad2dcf09148b5e1e4c14917fbf06265975a
      # - name: 🪄 Generate image
      #   env:
      #     POST_CARD_RECIEVER_NAME: ${{ secrets.YOUR_SECRET }}
      #     TEMPLATE_PATH: ""
      #   run: |
      #     convert -pointsize 60 -fill red -draw "text 800,1000 'TO: $POST_CARD_RECIEVER_NAME'" $TEMPLATE_PATH output/postcard_out.jpg


    # POST_CARD_RECIEVER_NAME: ${{ secrets.YOUR_SECRET }}
    #       TEMPLATE_PATH: ""